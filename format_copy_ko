sudo umount /mnt
sudo rmmod f2fs.ko
lsmod | grep f2fs
sudo rm /lib/modules/`uname -r`/kernel/fs/f2fs/f2fs.ko
ls -lh /lib/modules/`uname -r`/kernel/fs/f2fs/
#make
sudo cp fs/f2fs/f2fs.ko /lib/modules/`uname -r`/kernel/fs/f2fs/f2fs.ko
cd /github/f2fs-tools/mkfs
./mkfs.f2fs -t1 -a0 -m -f /dev/sdb
#cd /github/defrag-proof
#sudo ./test_case1.sh
sudo mount /mnt
cd /github/filebench-1.5-alpha3
sudo ./fix
#sudo ./benchmarks/cleantogether_measure.sh
sudo ./benchmarks/writeamplification.sh
# Keep the filesystem mounted for GC and benchmark to complete.
# Dont forget to calculate the layout score


